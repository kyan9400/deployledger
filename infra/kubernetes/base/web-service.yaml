apiVersion: v1
kind: Service
metadata:
  name: deployledger-web
  namespace: deployledger
  labels:
    app.kubernetes.io/name: deployledger-web
spec:
  selector:
    app.kubernetes.io/name: deployledger-web
  ports:
    - name: http
      port: 8080
      targetPort: http

