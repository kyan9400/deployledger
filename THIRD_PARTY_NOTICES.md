# Third-party notices

DeployLedger uses open-source dependencies listed in `services/api/pyproject.toml` and `apps/web/package.json`. Their licenses and notices remain with their respective projects. Production builds should generate a lockfile and dependency inventory as part of the release process.

